# Minimal .htaccess for symlinked deployment
# The main .htaccess is now in public/ directory

# Block access to the app directory itself (defense in depth)
<Files "*">
    Order Deny,Allow
    Deny from all
</Files>

# Allow only the public directory to be accessible via symlink
<Directory "public">
    Order Allow,Deny
    Allow from all
</Directory>